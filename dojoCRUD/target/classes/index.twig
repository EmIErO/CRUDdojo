<!doctype html>
<html>

<head></head>

<a href="students/add">ADD</a>
<div>
<table>
    <tr>
        <th>ID</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Age</th>
        <th>Actions</th>
    </tr>

    {% for student in students %}
        <tr>
            <td>{{student.ID}}</td>
            <td>{{student.firstName}}</td>
            <td>{{student.lastName}}</td>
            <td>{{student.age}}</td>
            <td><form method = post action="/students/edit/{{ loop.index }}"><button type="submit" >EDIT</button> </form>
                <form method = post action="/students/delete/{{ loop.index }}"><button type="submit" >EDELETE</button> </form>
        </tr>
        {% endfor %}


</table>
</div>

<body>

</body>
</html>